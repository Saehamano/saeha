<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goodsMapper">

	<!-- 굿즈 등록 -->
	<insert id="insert">
		INSERT INTO sae_goods(
		go_bno,
		go_name,
		go_content,
		go_file,
		go_price,
		go_stock,
		go_readcount
		)
		VALUES(
		'g'||TO_CHAR(sae_goods_seq.nextval),
		#{go_name},
		#{go_content},
		#{go_file},
		#{go_price},
		#{go_stock},
		#{go_readcount}
		)
	</insert>

	<!-- 굿즈 리스트 -->
	<select id="goodslist" resultType="goodsVO"
		parameterType="com.kh.saeha.vo.SearchCriteria">
		SELECT go_bno,
		go_name,
		go_file,
		go_price,
		go_stock,
		go_date,
		go_readcount
		FROM (
		SELECT
		go_bno,
		go_name,
		go_file,
		go_price,
		go_stock,
		go_date,
		go_readcount,
		ROW_NUMBER()
		OVER(ORDER BY go_date DESC) AS RNUM
		FROM sae_goods
		WHERE 1=1
		<include refid="search"></include>
		)GO
		WHERE RNUM BETWEEN #{rowStart} AND #{rowEnd}
		ORDER BY go_date DESC
	</select>

	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 'gn'.toString()">AND go_name LIKE '%' || #{keyword} || '%'</if>
		</if>
	</sql>

	<select id="listCount"
		parameterType="com.kh.saeha.vo.SearchCriteria" resultType="int">
		SELECT COUNT(go_bno)
		FROM sae_goods
		WHERE 1=1
		<include refid="search"></include>
	</select>

	<select id="read" resultType="goodsVO">
		SELECT go_bno
		, go_name
		, go_content
		, go_file
		, go_price
		, go_stock
		, go_date
		FROM sae_goods
		WHERE go_bno =
		#{go_bno}
	</select>
	
	<select id="readCount" resultType="goodsVO">
		update sae_goods
		set
		go_readcount = go_readcount + 1
		where
		go_bno = #{go_bno}
	</select>


</mapper>